def assign():
    IP = generate()
    find(IP)
    IPaddr = IPAdress(IP,False)
    Ipaddr.time()
    list.append(IP)
    return Iaddr
def find(IP):
    for i in range(0,list.length):
        if(list[i].adress == IP):
            if(isValidLease()):
                assign()
    return IP
